name: test

on:
  schedule:
    - cron:  '12 12 13 12 5'
  workflow_dispatch:
    inputs:
      models:
        description: "Models to run eg: test+ +test1 +test2+" 
        required: true
      environment:
        description: 'Prod'
        type: boolean
      full_refresh:
        description: 'full-refresh'
        type: boolean
      
      
  pull_request:
    branches:
      - main
    types: [opened,closed,edited, synchronize, reopened]

env:
  test: "test1"
  test2: "test2"

jobs:
  scheduled_run:
    name: scheduled_run
    runs-on: ubuntu-latest

    steps:
      # - name: checkout
      #   uses: actions/checkout@v2
      #   with:
      #     fetch-depth: 0
      
      # - name: log
      #   if: github.event.pull_request.merged == false
      #   run: |
      #     echo "${{ github.event.pull_request }}"

      # - name: run_models_test
      #   if: github.event.pull_request.merged == false
      #   run: |
      #     echo "NOT MERGED"
      
      # - name: run_models_prod
      #   if: github.event.pull_request.merged == true
      #   run: |
      #     echo "MERGED"
      #run: echo "${{ github.event.inputs.message }} ${{ fromJSON('["", "🥳"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"

      - name: test workflow dispatch
        if: github.event_name == 'workflow_dispatch'
        env: 
          test: "changed test"
        run: |
          echo "this is a workflow_dispatch"
          echo "dbt run ${{ github.event.inputs.models }} ${{ fromJSON('["","--target prod"]')[github.event.inputs.environment == 'true'] }} ${{ fromJSON('["","--full-refresh"]')[github.event.inputs.full_refresh == 'true'] }}"
      
      - name: pull request dev
        if: github.event_name == 'pull_request' && github.event.pull_request.merged == false
        run: |
          echo "dev"

      - name: merge prod
        if: github.event_name == 'pull_request' &&  github.event.pull_request.merged == true
        run: |
          echo "prod"


          
          
      